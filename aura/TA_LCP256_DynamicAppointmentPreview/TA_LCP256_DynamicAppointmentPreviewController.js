({
	handleInitialize : function(component, event, helper) {
        console.log('TA_LCP256_DynamicAppointmentPreview >> Controller >> handleInitialize >> Start');
        helper.initialize(component);
        console.log('TA_LCP256_DynamicAppointmentPreview >> Controller >> handleInitialize >> End');
    }
})