({
    handleInitialize : function(component, event, helper) {
        console.log('TA_LCP198_ProcessIndicator >> Controller >> handleInitialize >> Start');
        helper.initialize(component, event, helper);
        console.log('TA_LCP198_ProcessIndicator >> Controller >> handleInitialize >> End');
    }
})