({
    handleInitialize : function(component, event, helper) {
        console.log('TA_LCP237_RequiredDocument >> Controller >> handleInitialize >> Start');
        helper.initialize(component, event, helper);
        console.log('TA_LCP237_RequiredDocument >> Controller >> handleInitialize >> End');
    }
})