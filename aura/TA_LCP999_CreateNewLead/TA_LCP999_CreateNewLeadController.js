({
    handleInitialize : function(component, event, helper) {
        console.log('TA_LCP999_CreateNewLead >> Controller >> handleInitialize >> Start');
        helper.initialize(component, event, helper);
        console.log('TA_LCP999_CreateNewLead >> Controller >> handleInitialize >> End');
    }
})