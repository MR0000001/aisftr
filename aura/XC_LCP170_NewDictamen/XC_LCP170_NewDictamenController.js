/**
* @author Salvatore Agrillo - salvatore.agrillo@nttdata.com
* @date Creation 06/11/2019
* @description Controller class for component XC_LCP170_NewDictamen
*/

({
    doInit : function(component, event, helper) {
        helper.init(component, helper, event);
    },

    createDictamen : function(component, event, helper) {
        helper.createDictamen(component, helper, event);
    }
})