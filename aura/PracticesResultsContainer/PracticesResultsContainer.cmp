<aura:component implements="flexipage:availableForAllPageTypes,lightning:isUrlAddressable" access="global">
    <aura:attribute name="practices" type="String"/>
    <aura:attribute name="practiceCodeIndicated" type="String"/>
    <aura:attribute name="fiscalCode" type="String"/>
    <aura:attribute name="startPage" type="Integer"/>
    <aura:attribute name="endPage" type="Integer"/>
    <aura:attribute name="totalrecords" type="Integer"/>
    <aura:attribute name="totalPages" type="Integer"/>

    <lightning:workspaceAPI aura:id="workspace"/>
    <aura:handler name="init" value="{!this}" action="{!c.init}"/>    

    <div class="slds-card">
        <c:practiceResult practicesString="{!v.practices}" practiceCodeIndicated="{!v.practiceCodeIndicated}" 
        startPage="{!v.startPage}" endPage="{!v.endPage}" totalrecords="{!v.totalrecords}" totalpages="{!v.totalPages}"
         fiscalCode="{!v.fiscalCode}"
        />
    </div>
</aura:component>